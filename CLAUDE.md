# CLAUDE.md

This file provides guidance to Claude Code (and other AI assistants) when working
with code in this repository.

> **Status: This repository is currently empty** (no source files committed yet).
> This document is a starter scaffold. As the project takes shape, replace the
> placeholder sections below with accurate, specific details. Keep this file in
> sync with the actual state of the codebase.

## Project Overview

> _TODO:_ Describe what this project is, who it's for, and the problem it solves.
> Include the primary language(s), framework(s), and runtime/version requirements
> once they are chosen.

- **Name:** New-Jersey
- **Purpose:** _TODO_
- **Tech stack:** _TODO_

## Repository Structure

> _TODO:_ Document the top-level layout once code exists. Example format:
>
> ```text
> .
> ├── src/          # Application source
> ├── tests/        # Test suite
> ├── docs/         # Documentation
> └── ...
> ```

## Development Workflow

### Setup

> _TODO:_ Commands to install dependencies and prepare a local environment.
> Example: `npm install`, `pip install -e .`, `make setup`, etc.

### Build

> _TODO:_ How to build the project, if applicable.

### Run

> _TODO:_ How to start the app or run the main entry point locally.

### Test

> _TODO:_ How to run the test suite (and a single test). AI assistants should
> run these before considering a change complete.

### Lint / Format

> _TODO:_ Linting and formatting commands and the tools used. Run these before
> committing.

## Conventions

> _TODO:_ Capture project-specific conventions so changes match the existing
> style. Consider documenting:
>
> - Code style / formatting rules (and the tool that enforces them)
> - Naming conventions
> - Commit message format and branching strategy
> - How configuration and secrets are handled
> - Any patterns to follow or anti-patterns to avoid

## Notes for AI Assistants

- This file is the source of truth for how to work in this repo — update it
  whenever workflows, structure, or conventions change.
- Prefer matching the surrounding code's style and idioms over introducing new
  patterns.
- Run the test and lint commands (once defined above) before finishing a change.
- Do not create pull requests unless explicitly asked.
